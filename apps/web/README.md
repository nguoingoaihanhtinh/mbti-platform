---
# MBTI Platform – Frontend

Ứng dụng frontend cho nền tảng MBTI, xây dựng bằng **React + Vite**, hỗ trợ đầy đủ 4 cổng người dùng với phân quyền rõ ràng.
---

## 🛠️ Công nghệ sử dụng

- **Framework**: React 18 + TypeScript
- **Routing**: TanStack Router
- **State Management**: Zustand + Persist Middleware
- **Styling**: TailwindCSS + Custom UI
- **Data Fetching**: TanStack Query
- **Build Tool**: Vite + SWC (nhanh hơn Babel ~20x)
- **Linting**: ESLint (`@repo/eslint-config`)

---

## ▶️ Chạy ứng dụng

### Development (local)

#### Chạy toàn bộ monorepo (từ thư mục gốc)

```bash
bun run dev
```
